{
  "name": "vr-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc ./src/lambda/index.ts",
    "deploy": "npm run build && sls deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "~2.1343.0",
    "typescript": "~5.0.2"
  },
  "devDependencies": {
    "@types/node": "~18.15.9"
  }
}
